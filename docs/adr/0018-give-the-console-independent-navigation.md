# Give the console independent navigation

The React console will own its application shell and navigation instead of embedding HTML generated by `brpc::Server::PrintTabsBody`. It will retain a clearly marked Server Tools link to bRPC's native diagnostics, preserving those operational capabilities without coupling the React root document, tests, or content-security policy to server-generated tab markup.
