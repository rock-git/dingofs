# Migrate core resource lists with the overview

The first console increment will include the overview plus React resource-list routes for file systems, MDS nodes, clients, and cache members. This prevents the bounded overview previews from removing information that the current long page exposes; lower-frequency diagnostics and detail pages may retain legacy entry points until they are migrated separately.
